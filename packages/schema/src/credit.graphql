# import ScreenType from "enums.graphql"
# import Node from "interfaces.graphql"
# import Person from "person.graphql"
# import Screen from "unions.graphql"

type Credit implements Node {
  creditType: String!
  department: String!
  id: ID!
  job: String!
  media: Screen!
  mediaType: ScreenType!
  person: Person!
}
