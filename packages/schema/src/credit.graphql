# import Person from "person.graphql"
# import Media from "unions.graphql"

type Credit {
  creditType: String!
  department: String!
  id: ID!
  job: String!
  media: Media
  mediaType: String!
  person: Person!
}
