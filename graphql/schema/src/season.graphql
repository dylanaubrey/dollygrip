# import Node from "interfaces.graphql"
# import Date from "scalars.graphql"

type Season implements Node {
  airDate: Date!
  episodeCount: Int!
  id: ID!
  name: String!
  overview: String!
  posterPath: String
  seasonNumber: Int!
}
