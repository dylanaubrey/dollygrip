# import Connection from "interfaces.graphql"
# import Review from "review.graphql"
# import PageInfo from "page-info.graphql"

type ReviewEdge {
  cursor: String!
  node: Review
}

type ReviewConnection implements Connection {
  edges: [ReviewEdge]
  pageInfo: PageInfo!
  totalResults: Int!
}
