# import Node from "interfaces.graphql"
# import VideoType from "enums.graphql"

type Video implements Node {
  id: ID!
  iso6391: String!
  iso31661: String!
  key: String!
  name: String!
  site: String!
  size: Int!
  type: VideoType!
}
